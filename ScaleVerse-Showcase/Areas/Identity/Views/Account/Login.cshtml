@using ScalaVerse.ViewModel.Identity_VM
@model LoginViewModel
@{
    ViewData["Title"] = "Sign In";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign In — ScaleVerse</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <style>
        *, *::before, *::after {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        :root {
            --navy: #0A0E1A;
            --purple: #7C3AED;
            --purple-2: #6D28D9;
            --purple-3: #8B5CF6;
            --purple-4: #A78BFA;
            --f-display: 'Plus Jakarta Sans', sans-serif;
            --f-body: 'Inter', sans-serif;
            --ease: cubic-bezier(0.16, 1, 0.3, 1);
        }

        html, body {
            font-family: var(--f-body);
            -webkit-font-smoothing: antialiased;
            background: var(--navy);
        }

        .page {
            min-height: 100vh;
            background: var(--navy);
            display: grid;
            grid-template-rows: auto 1fr auto;
            position: relative;
            overflow: hidden;
        }

            .page::before {
                content: '';
                position: fixed;
                inset: 0;
                z-index: 0;
                pointer-events: none;
                background-image: linear-gradient(rgba(124,58,237,0.055) 1px, transparent 1px), linear-gradient(90deg, rgba(124,58,237,0.055) 1px, transparent 1px);
                background-size: 48px 48px;
            }

            .page::after {
                content: '';
                position: fixed;
                top: -220px;
                right: -180px;
                z-index: 0;
                pointer-events: none;
                width: 640px;
                height: 640px;
                border-radius: 50%;
                background: radial-gradient(ellipse, rgba(124,58,237,0.16) 0%, transparent 65%);
            }

        .page-glow2 {
            position: fixed;
            bottom: -180px;
            left: -180px;
            z-index: 0;
            pointer-events: none;
            width: 500px;
            height: 500px;
            border-radius: 50%;
            background: radial-gradient(ellipse, rgba(124,58,237,0.08) 0%, transparent 65%);
        }

        /* NAV */
        .auth-nav {
            position: relative;
            z-index: 10;
            padding: 18px 48px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid rgba(124,58,237,0.1);
        }

        .nav-logo {
            display: flex;
            align-items: center;
            text-decoration: none;
        }

            .nav-logo img {
                height: 90px;
                width: auto;
                object-fit: contain;
                display: block;
            }

        .nav-badge {
            margin-left: 10px;
            font-size: .65rem;
            font-weight: 700;
            letter-spacing: .08em;
            text-transform: uppercase;
            color: var(--purple-4);
            border: 1px solid rgba(167,139,250,0.3);
            border-radius: 20px;
            padding: 2px 8px;
            background: rgba(124,58,237,0.08);
        }

        .nav-right {
            font-size: .83rem;
            color: rgba(255,255,255,.38);
        }

            .nav-right a {
                color: var(--purple-4);
                font-weight: 600;
                text-decoration: none;
                transition: color .2s;
            }

                .nav-right a:hover {
                    color: #fff;
                }

        /* MAIN */
        .auth-main {
            position: relative;
            z-index: 5;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 32px 24px 40px;
        }

        /* CARD */
        .auth-card {
            width: 100%;
            max-width: 440px;
            background: transparent;
            border-radius: 20px;
            border: 1px solid rgba(124,58,237,0.18);
            overflow: hidden;
            animation: cardUp 0.65s var(--ease) both;
            box-shadow: 0 32px 80px rgba(0,0,0,0.35), inset 0 1px 0 rgba(255,255,255,0.04);
        }

        @@keyframes cardUp {
            from {
                opacity: 0;
                transform: translateY(28px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .card-accent {
            height: 3px;
            background: linear-gradient(90deg, var(--purple) 0%, var(--purple-3) 55%, #C4B5FD 100%);
        }

        .card-body {
            padding: 36px 36px 32px;
        }

        .card-header-wrap {
            margin-bottom: 26px;
        }

        .card-eyebrow {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            font-size: .68rem;
            font-weight: 700;
            letter-spacing: .1em;
            text-transform: uppercase;
            color: var(--purple-4);
            margin-bottom: 10px;
        }

            .card-eyebrow::before {
                content: '';
                width: 16px;
                height: 1.5px;
                background: var(--purple-4);
                border-radius: 2px;
            }

        .card-title {
            font-family: var(--f-display);
            font-size: 1.5rem;
            font-weight: 800;
            letter-spacing: -.03em;
            color: #fff;
            margin-bottom: 5px;
        }

        .card-sub {
            font-size: .84rem;
            color: rgba(255,255,255,.38);
            line-height: 1.5;
        }

        /* FIELDS */
        .field {
            margin-bottom: 16px;
        }

        .field-label {
            display: block;
            font-size: .8rem;
            font-weight: 600;
            color: rgba(255,255,255,.72);
            margin-bottom: 6px;
        }

        .field-input {
            width: 100%;
            padding: 11px 14px;
            border: 1.5px solid rgba(124,58,237,0.18);
            border-radius: 10px;
            font-family: var(--f-body);
            font-size: .88rem;
            color: #F9FAFB;
            background: rgba(124,58,237,0.06);
            outline: none;
            transition: all .18s;
        }

            .field-input:focus {
                border-color: var(--purple);
                background: rgba(124,58,237,0.1);
                box-shadow: 0 0 0 3px rgba(124,58,237,0.15);
            }

            .field-input::placeholder {
                color: rgba(255,255,255,.18);
            }

        .field-error {
            font-size: .74rem;
            color: #FCA5A5;
            margin-top: 4px;
            display: block;
        }

        .val-summary {
            background: rgba(220,38,38,0.08);
            border: 1px solid rgba(220,38,38,0.25);
            border-radius: 10px;
            padding: 10px 14px;
            font-size: .8rem;
            color: #FCA5A5;
            margin-bottom: 18px;
            display: none;
        }

            .val-summary ul {
                margin: 0;
                padding-left: 14px;
            }

        .field-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 22px;
        }

        .check-wrap {
            display: flex;
            align-items: center;
            gap: 7px;
        }

            .check-wrap input[type="checkbox"] {
                width: 15px;
                height: 15px;
                accent-color: var(--purple);
                cursor: pointer;
            }

        .check-label {
            font-size: .8rem;
            color: rgba(255,255,255,.38);
            cursor: pointer;
        }

        .forgot-link {
            font-size: .8rem;
            font-weight: 600;
            color: var(--purple-4);
            text-decoration: none;
            transition: color .2s;
        }

            .forgot-link:hover {
                color: #fff;
            }

        .btn-submit {
            width: 100%;
            padding: 13px;
            background: var(--purple);
            color: #fff;
            font-family: var(--f-body);
            font-size: .92rem;
            font-weight: 700;
            border: none;
            border-radius: 11px;
            cursor: pointer;
            transition: all .25s var(--ease);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            margin-bottom: 20px;
            box-shadow: 0 4px 20px rgba(124,58,237,0.4);
            letter-spacing: .01em;
        }

            .btn-submit:hover {
                background: var(--purple-2);
                transform: translateY(-1px);
                box-shadow: 0 8px 28px rgba(124,58,237,0.5);
            }

        .btn-arrow {
            font-size: 1rem;
            transition: transform .2s;
        }

        .btn-submit:hover .btn-arrow {
            transform: translateX(3px);
        }

        .auth-switch {
            text-align: center;
            font-size: .83rem;
            color: rgba(255,255,255,.3);
        }

            .auth-switch a {
                font-weight: 700;
                color: var(--purple-4);
                text-decoration: none;
                transition: color .2s;
            }

                .auth-switch a:hover {
                    color: #fff;
                }

        /* FOOTER */
        .auth-footer {
            position: relative;
            z-index: 5;
            text-align: center;
            padding: 20px;
            font-size: .71rem;
            color: rgba(255,255,255,.15);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 16px;
        }

            .auth-footer a {
                color: rgba(255,255,255,.2);
                text-decoration: none;
                transition: color .2s;
            }

                .auth-footer a:hover {
                    color: rgba(255,255,255,.5);
                }

        .footer-dot {
            width: 3px;
            height: 3px;
            border-radius: 50%;
            background: rgba(255,255,255,.1);
        }

        @@media (max-width: 480px) {
            .auth-nav {
                padding: 16px 20px;
            }

            .card-body {
                padding: 28px 22px 24px;
            }

            .nav-badge {
                display: none;
            }
        }

        .nav-cta-outline {
            display: inline-flex;
            align-items: center;
            padding: 9px 20px;
            background: transparent;
            color: var(--purple-4) !important;
            font-size: .83rem;
            font-weight: 700;
            border-radius: 10px;
            border: 1.5px solid rgba(124,58,237,0.45);
            text-decoration: none;
            letter-spacing: .01em;
            box-shadow: 0 0 0 0 rgba(124,58,237,0.4);
            animation: navPulse 2.5s ease-in-out infinite;
            transition: all .2s;
        }

            .nav-cta-outline:hover {
                background: rgba(124,58,237,0.1);
                border-color: var(--purple);
                color: #fff !important;
                transform: translateY(-1px);
                animation: none;
            }
    </style>
</head>
<body>
    <div class="page">
        <div class="page-glow2"></div>

        <nav class="auth-nav">
            <a href="/" class="nav-logo">
                <img src="@Url.Content("~/uploads/companies/logos/scalaverselogo.png")" alt="ScaleVerse" />
                
            </a>
            <div class="nav-right">
                <a asp-area="Identity" asp-controller="Account" asp-action="Register" class="nav-cta-outline">
                    Sign Up
                </a>
            </div>
        </nav>

        <main class="auth-main">
            <div class="auth-card">
                <div class="card-accent"></div>
                <div class="card-body">

                    <div class="card-header-wrap">
                        <div class="card-eyebrow">Welcome back</div>
                        <div class="card-title">Sign in to ScaleVerse</div>
                        <div class="card-sub">Enter your credentials to access your account</div>
                    </div>

                    <form asp-action="Login" asp-controller="Account" asp-area="Identity" method="post">

                        <div asp-validation-summary="ModelOnly" class="val-summary"></div>

                        <div class="field">
                            <label asp-for="Email" class="field-label">Email Address</label>
                            <input asp-for="Email" type="email" class="field-input" placeholder="name@company.com" />
                            <span asp-validation-for="Email" class="field-error"></span>
                        </div>

                        <div class="field">
                            <label asp-for="Password" class="field-label">Password</label>
                            <input asp-for="Password" type="password" class="field-input" placeholder="Your password" />
                            <span asp-validation-for="Password" class="field-error"></span>
                        </div>

                        <div class="field-row">
                            <div class="check-wrap">
                                <input asp-for="RememberMe" type="checkbox" id="rememberMe" />
                                <label class="check-label" for="rememberMe">Remember me</label>
                            </div>
                            <a asp-action="ForgotPassword" asp-controller="Account" asp-area="Identity" class="forgot-link">Forgot password?</a>
                        </div>

                        <button type="submit" class="btn-submit">
                            Sign In <span class="btn-arrow">→</span>
                        </button>

                        <div class="auth-switch">
                            New to ScaleVerse?
                            <a asp-action="Register" asp-controller="Account" asp-area="Identity">Create account</a>
                        </div>

                    </form>
                </div>
            </div>
        </main>

        <footer class="auth-footer">
            <span>© 2026 ScaleVerse. All rights reserved.</span>
            <span class="footer-dot"></span>
            <a href="#">Privacy</a>
            <span class="footer-dot"></span>
            <a href="#">Terms</a>
            <span class="footer-dot"></span>
            <a href="#">Support</a>
        </footer>

    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const vs = document.querySelector('.val-summary');
        if (vs && vs.querySelector('li')) vs.style.display = 'block';
    </script>
    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
</body>
</html>